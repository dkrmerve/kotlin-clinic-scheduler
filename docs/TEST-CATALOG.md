# Test catalog

Generated by `scripts/test-catalog.py` from the JUnit XML of the last `./gradlew test` (H2) and `./gradlew integrationTest`
(PostgreSQL via Testcontainers) runs. Re-run it after adding tests; the suite descriptions live in the script.
Data-driven tests (`withData`) appear once per row of their table.

**321 distinct tests.** Database column: which suite runs each test (skipped runs are not listed).

## domain.SchedulingRulesTest

Rules 1-6 and 8 as pure functions with a fixed clock: boundary tables (withData), property tests for slot alignment and interval intersection, availability generation.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| start exactly at opening 09:00 -> allowed | rule 1 | none (pure unit test) |
| consultation ending exactly at closing 17:00 -> allowed | rule 1 | none (pure unit test) |
| follow-up ending exactly at closing 17:00 -> allowed | rule 1 | none (pure unit test) |
| procedure that would end at 17:30 -> outside_working_hours | `outside_working_hours` | none (pure unit test) |
| consultation that would end at 17:15 -> outside_working_hours | `outside_working_hours` | none (pure unit test) |
| start at closing time 17:00 -> outside_working_hours | rule 1, `outside_working_hours` | none (pure unit test) |
| start before opening 08:45 -> outside_working_hours | rule 1, `outside_working_hours` | none (pure unit test) |
| start off the 15-minute grid 09:10 -> slot_misaligned | rule 1, `slot_misaligned` | none (pure unit test) |
| start with seconds 09:00:30 -> slot_misaligned | rule 1, `slot_misaligned` | none (pure unit test) |
| a weekday without a working window (Sunday) -> outside_working_hours | rule 1, `outside_working_hours` | none (pure unit test) |
| ending one minute after closing is rejected while ending exactly at closing is allowed | rule 1 | none (pure unit test) |
| property: every start on the 15-minute grid whose consultation ends by closing time is aligned | rule 1 | none (pure unit test) |
| property: any start strictly between grid points is slot_misaligned | rule 1, `slot_misaligned` | none (pure unit test) |
| slot boundaries are counted from the window start, not from the hour | rule 1 | none (pure unit test) |
| new start exactly at existing.end + buffer (10:40) -> allowed | rule 2 | none (pure unit test) |
| new start one minute inside the trailing buffer (10:39) -> slot_taken | rule 2, `slot_taken` | none (pure unit test) |
| new start at existing.end without buffer (10:30) -> slot_taken | rule 2, `slot_taken` | none (pure unit test) |
| new end exactly at existing.start - buffer (09:20-09:50) -> allowed | rule 2 | none (pure unit test) |
| new end one minute inside the leading buffer (09:21-09:51) -> slot_taken | rule 2, `slot_taken` | none (pure unit test) |
| identical slot -> slot_taken | `slot_taken` | none (pure unit test) |
| with bufferMinutes = 0 two back-to-back appointments touch without overlapping | rule 2 | none (pure unit test) |
| existing Booked appointment blocks the slot | - | none (pure unit test) |
| existing CheckedIn appointment blocks the slot | - | none (pure unit test) |
| existing Completed appointment blocks the slot | - | none (pure unit test) |
| existing NoShow appointment frees the slot | - | none (pure unit test) |
| existing Cancelled by patient appointment frees the slot | rule 7 | none (pure unit test) |
| existing Cancelled by clinic appointment frees the slot | rule 7 | none (pure unit test) |
| property: interval intersection is symmetric and matches the half-open definition | - | none (pure unit test) |
| an appointment overlapping the block is rejected with the reason in the detail | rule 2 | none (pure unit test) |
| an appointment ending exactly when the block starts is allowed | - | none (pure unit test) |
| an appointment starting exactly when the block ends is allowed | - | none (pure unit test) |
| start == now -> outside_booking_horizon | rule 4, `outside_booking_horizon` | none (pure unit test) |
| start one second in the past -> outside_booking_horizon | rule 4, `outside_booking_horizon` | none (pure unit test) |
| start one second in the future -> allowed | - | none (pure unit test) |
| start exactly now + 60 days -> allowed | - | none (pure unit test) |
| start now + 60 days + 1 minute -> outside_booking_horizon | rule 4, `outside_booking_horizon` | none (pure unit test) |
| today and lastBookableDate are evaluated in the clinic zone | - | none (pure unit test) |
| a second appointment with the same practitioner on the same clinic day is rejected | - | none (pure unit test) |
| an overlapping appointment with another practitioner is rejected | rule 2 | none (pure unit test) |
| another practitioner at a different time on the same day is allowed | - | none (pure unit test) |
| the same practitioner on the next day is allowed | - | none (pure unit test) |
| cancelled and no-show appointments do not count | rule 8, rule 7 | none (pure unit test) |
| the maximum is reached -> rejected | - | none (pure unit test) |
| a cancelled appointment frees capacity, a no-show does not | rule 8, rule 7, rule 6 | none (pure unit test) |
| appointments on other days do not count | - | none (pure unit test) |
| a patient blocked until the future cannot book; the block end travels with the exception | rule 8 | none (pure unit test) |
| at blockedUntil exactly the patient is free again | rule 8 | none (pure unit test) |
| a patient that was never blocked passes | rule 8 | none (pure unit test) |
| before the start time -> rejected | - | none (pure unit test) |
| exactly at the start time -> still rejected (start must have passed) | - | none (pure unit test) |
| after the start time -> allowed | - | none (pure unit test) |
| a clean context passes | - | none (pure unit test) |
| the block check comes first, before the horizon | rule 4 | none (pure unit test) |
| each later rule is reachable through validateBooking | - | none (pure unit test) |
| a plain Tuesday on a 15-minute grid offers 31 consultation starts from 09:00 to 16:30 | - | none (pure unit test) |
| starts whose appointment would end after closing are filtered (procedure: last start 16:00) | rule 1 | none (pure unit test) |
| a day without a working window has no slots | rule 1 | none (pure unit test) |
| a full-day time-off block empties the day | rule 3 | none (pure unit test) |
| existing appointments remove their own slot and the buffered neighbours | rule 2 | none (pure unit test) |
| once maxAppointmentsPerDay is reached the day is empty | - | none (pure unit test) |
| slots already in the past today are not offered (now is Monday 09:00, so 09:15 is the first) | - | none (pure unit test) |
| a window ending just before midnight stops cleanly instead of wrapping | rule 1 | none (pure unit test) |

## domain.DstTest

Both Europe/Amsterdam DST switches of 2027: slot enumeration in wall time, instants for duplicated/skipped hours, real durations across the switch.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| a 09:00-17:00 window still offers 31 consultation slots, the first at 09:00+02:00 | rule 1 | none (pure unit test) |
| every consecutive pair of slots is exactly 15 real minutes apart | - | none (pure unit test) |
| an overnight 01:00-04:00 window skips the missing hour and yields no duplicate instants | rule 1 | none (pure unit test) |
| a 30-minute appointment booked at 01:45+01:00 crosses the switch and ends at 03:15+02:00, 30 real minutes later | - | none (pure unit test) |
| the last slot of the overnight window still respects the 04:00 closing time in instants | rule 1 | none (pure unit test) |
| a 09:00-17:00 window offers the normal 31 consultation slots at +01:00 | rule 1 | none (pure unit test) |
| an overnight 01:00-04:00 window offers both occurrences of 02:00 and 02:30 as distinct slots | rule 1 | none (pure unit test) |
| appointments at the two occurrences of 02:00 do not overlap each other | rule 2 | none (pure unit test) |
| an appointment starting at 02:45+02:00 ends at 02:15+01:00 and still lasts 30 real minutes | - | none (pure unit test) |
| the clinic day bounds cover 25 hours on the fall-back day | DST | none (pure unit test) |

## domain.CancellationPolicyTest

Rule 7 at exact boundaries (24 h, 24 h - 1 s, 2 h, 2 h - 1 s), clinic cancellations, configurable notice periods.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| 48h before -> free | - | none (pure unit test) |
| exactly 24h before -> free | - | none (pure unit test) |
| 24h minus 1s before -> late | - | none (pure unit test) |
| 3h before -> late | - | none (pure unit test) |
| exactly 2h before -> late | - | none (pure unit test) |
| 2h minus 1s before -> cancellation_window_closed | rule 7, rule 1, `cancellation_window_closed` | none (pure unit test) |
| 1 minute before -> cancellation_window_closed | rule 7, rule 1, `cancellation_window_closed` | none (pure unit test) |
| after the start -> cancellation_window_closed | rule 7, rule 1, `cancellation_window_closed` | none (pure unit test) |
| 1 minute before the start is allowed and never marked late | - | none (pure unit test) |
| even after the start the clinic may cancel | rule 7 | none (pure unit test) |
| a policy with a 48h free window and 12h minimum notice moves both boundaries | rule 1 | none (pure unit test) |

## domain.NoShowPolicyTest

Rule 8: rolling 90-day window (inclusive), retroactive no-shows, blockedUntil boundary, policy knobs.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| third no-show exactly 90 days after the first -> blocked | rule 8 | none (pure unit test) |
| third no-show 89 days after the first -> blocked | rule 8 | none (pure unit test) |
| third no-show 91 days (first drops out of the window) after the first -> not blocked | rule 8, rule 1 | none (pure unit test) |
| two no-shows never block | rule 8 | none (pure unit test) |
| the block is computed from history, so a retroactively recorded older no-show still triggers it | rule 11, rule 8 | none (pure unit test) |
| an existing block is kept when a later no-show does not reach the limit again | rule 8 | none (pure unit test) |
| one second before blockedUntil -> blocked | rule 8 | none (pure unit test) |
| at blockedUntil exactly -> free again | rule 8 | none (pure unit test) |
| never blocked -> free | rule 8 | none (pure unit test) |
| late cancellations are counted, not blocked | rule 8, rule 7 | none (pure unit test) |
| a stricter policy (limit 2, window 7 days, block 1 day) is honoured | rule 1 | none (pure unit test) |

## domain.AppointmentStateMachineTest

Rule 10 as a full transition matrix (every status pair) and rule 11 history entries.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| Booked -> Booked: invalid_transition | `invalid_transition` | none (pure unit test) |
| Booked -> CheckedIn: allowed | - | none (pure unit test) |
| Booked -> Completed: invalid_transition | `invalid_transition` | none (pure unit test) |
| Booked -> NoShow: allowed | - | none (pure unit test) |
| Booked -> Cancelled(patient): allowed | rule 7 | none (pure unit test) |
| Booked -> Cancelled(clinic): allowed | rule 7 | none (pure unit test) |
| CheckedIn -> Booked: invalid_transition | `invalid_transition` | none (pure unit test) |
| CheckedIn -> CheckedIn: invalid_transition | `invalid_transition` | none (pure unit test) |
| CheckedIn -> Completed: allowed | - | none (pure unit test) |
| CheckedIn -> NoShow: invalid_transition | `invalid_transition` | none (pure unit test) |
| CheckedIn -> Cancelled(patient): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| CheckedIn -> Cancelled(clinic): allowed | rule 7 | none (pure unit test) |
| Completed -> Booked: invalid_transition | `invalid_transition` | none (pure unit test) |
| Completed -> CheckedIn: invalid_transition | `invalid_transition` | none (pure unit test) |
| Completed -> Completed: invalid_transition | `invalid_transition` | none (pure unit test) |
| Completed -> NoShow: invalid_transition | `invalid_transition` | none (pure unit test) |
| Completed -> Cancelled(patient): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Completed -> Cancelled(clinic): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| NoShow -> Booked: invalid_transition | `invalid_transition` | none (pure unit test) |
| NoShow -> CheckedIn: invalid_transition | `invalid_transition` | none (pure unit test) |
| NoShow -> Completed: invalid_transition | `invalid_transition` | none (pure unit test) |
| NoShow -> NoShow: invalid_transition | `invalid_transition` | none (pure unit test) |
| NoShow -> Cancelled(patient): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| NoShow -> Cancelled(clinic): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> Booked: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> CheckedIn: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> Completed: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> NoShow: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> Cancelled(patient): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(patient) -> Cancelled(clinic): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> Booked: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> CheckedIn: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> Completed: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> NoShow: invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> Cancelled(patient): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| Cancelled(clinic) -> Cancelled(clinic): invalid_transition | rule 7, `invalid_transition` | none (pure unit test) |
| CheckedIn -> Cancelled by patient is the one cancellation the state machine refuses | rule 7 | none (pure unit test) |
| booking writes the first entry with no previous status | - | none (pure unit test) |
| every transition appends (at, actor, from, to, note) in order | - | none (pure unit test) |
| a refused transition leaves the appointment and its history untouched | rule 11 | none (pure unit test) |
| Booked: blocksSlot=true countsTowardsCapacity=true | rule 6 | none (pure unit test) |
| CheckedIn: blocksSlot=true countsTowardsCapacity=true | rule 6 | none (pure unit test) |
| Completed: blocksSlot=true countsTowardsCapacity=true | rule 6 | none (pure unit test) |
| NoShow: blocksSlot=false countsTowardsCapacity=true | rule 6 | none (pure unit test) |
| Cancelled(patient): blocksSlot=false countsTowardsCapacity=false | rule 7, rule 6 | none (pure unit test) |
| Cancelled(clinic): blocksSlot=false countsTowardsCapacity=false | rule 7, rule 6 | none (pure unit test) |
| labels are the wire representation | - | none (pure unit test) |

## domain.EntityInvariantsTest

Every `invariant` in the aggregates throws a typed ValidationException with its code; ids, types, time helpers, exception catalog.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| a valid practitioner exposes its buffer as a Duration | rule 2 | none (pure unit test) |
| slotMinutes 10 -> slot_incompatible | `slot_incompatible` | none (pure unit test) |
| slotMinutes 20 -> slot_incompatible | `slot_incompatible` | none (pure unit test) |
| slotMinutes 30 -> slot_incompatible | `slot_incompatible` | none (pure unit test) |
| slotMinutes 45 -> invalid_practitioner | `invalid_practitioner` | none (pure unit test) |
| slot 15 is compatible with every appointment type | - | none (pure unit test) |
| blank name, blank specialty, negative buffer and zero capacity are invalid_practitioner | rule 6, rule 2, `invalid_practitioner` | none (pure unit test) |
| a working window shorter than one slot is invalid_schedule | rule 1, `invalid_schedule` | none (pure unit test) |
| start must be before end | - | none (pure unit test) |
| length and minutesFromStart | - | none (pure unit test) |
| an empty schedule is invalid_schedule; lookup by weekday returns null for days off | `invalid_schedule` | none (pure unit test) |
| blank name -> invalid_patient | `invalid_patient` | none (pure unit test) |
| email 'no-at.example.test' is rejected | - | none (pure unit test) |
| email 'a@b' is rejected | - | none (pure unit test) |
| email 'a b@example.test' is rejected | - | none (pure unit test) |
| email '@example.test' is rejected | - | none (pure unit test) |
| email '' is rejected | - | none (pure unit test) |
| negative lateCancellations -> invalid_patient | rule 7, `invalid_patient` | none (pure unit test) |
| must start before it ends and needs a reason | - | none (pure unit test) |
| intersects is half-open on both sides | - | none (pure unit test) |
| end must equal start + duration of the type | - | none (pure unit test) |
| expires lazily once its date is in the past, and only while waiting | - | none (pure unit test) |
| evaluatedOn returns the same instance when nothing changes | - | none (pure unit test) |
| fulfilling records the appointment; fulfilling twice is an invalid transition | - | none (pure unit test) |
| defaults | - | none (pure unit test) |
| invalid policy: zero horizon | rule 4 | none (pure unit test) |
| invalid policy: negative minimum notice | - | none (pure unit test) |
| invalid policy: minimum notice above free notice | - | none (pure unit test) |
| invalid policy: no-show limit 0 | rule 8 | none (pure unit test) |
| invalid policy: zero no-show window | rule 8, rule 1 | none (pure unit test) |
| invalid policy: zero block | - | none (pure unit test) |
| value class ids round-trip through their string form | - | none (pure unit test) |
| appointment types carry fixed durations | - | none (pure unit test) |
| clinicDate and inZone follow the clinic zone, toInstantIn goes back | - | none (pure unit test) |
| every factory yields the documented code and a non-blank detail | - | none (pure unit test) |

## api.AuthTest

JWT authentication (401 for missing/expired/foreign-key/malformed tokens) and role/ownership authorization (403).

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| no token | auth | H2 + PostgreSQL |
| expired token | auth | H2 + PostgreSQL |
| token signed with another key | auth | H2 + PostgreSQL |
| token with an unknown role claim or a blank subject is rejected | auth | H2 + PostgreSQL |
| a malformed Authorization header | - | H2 + PostgreSQL |
| POST /practitioners requires admin: staff and patients are refused | - | H2 + PostgreSQL |
| POST /patients, time-off, check-in, complete, no-show and GET /waitlist are staff-or-admin only | rule 9, rule 8, rule 3 | H2 + PostgreSQL |
| a patient can read and act on their own records but not on another patient's | - | H2 + PostgreSQL |
| staff and admin pass every ownership check | auth | H2 + PostgreSQL |
| a staff cancellation is a clinic cancellation (never late), a patient cancellation follows the policy | rule 7 | H2 + PostgreSQL |
| a token without an exp claim is rejected with 401 (expiry is mandatory, not optional) | auth | H2 + PostgreSQL |

## api.PractitionerApiTest

POST/GET practitioners, time-off, availability endpoint including horizon and validation errors, Location headers.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| creates a practitioner -> 201 with Location header, then GET returns the same representation | 201 Location | H2 + PostgreSQL |
| collects every field error in one 400 validation_failed response | `validation_failed` | H2 + PostgreSQL |
| an empty schedule is a field error | - | H2 + PostgreSQL |
| slotMinutes 30 is in the allowed set but cannot fit a 15-minute follow-up -> 400 slot_incompatible | `slot_incompatible` | H2 + PostgreSQL |
| a working window shorter than one slot -> 400 invalid_schedule | rule 1, `invalid_schedule` | H2 + PostgreSQL |
| unknown id -> 404 practitioner_not_found; non-UUID id -> 404 as well | `practitioner_not_found` | H2 + PostgreSQL |
| creates a block -> 201 with Location of the practitioner, and availability honours it | 201 Location | H2 + PostgreSQL |
| validation: from >= to, missing offset and blank reason are all reported | - | H2 + PostgreSQL |
| unknown practitioner -> 404 | - | H2 + PostgreSQL |
| a free Tuesday offers 31 consultation slots in clinic-zone ISO format | - | H2 + PostgreSQL |
| slots that would end after the window are filtered (procedures stop at 16:00) | rule 1 | H2 + PostgreSQL |
| existing appointments and their buffer disappear from availability | rule 2 | H2 + PostgreSQL |
| a full-day time-off block empties the day | rule 3 | H2 + PostgreSQL |
| once maxAppointmentsPerDay is reached the day is empty | - | H2 + PostgreSQL |
| a day off (Saturday) has no slots | - | H2 + PostgreSQL |
| date in the past -> 422 outside_booking_horizon | rule 4, `outside_booking_horizon` | H2 + PostgreSQL |
| date beyond 60 days -> 422 outside_booking_horizon, exactly 60 days ahead is fine | rule 4, `outside_booking_horizon` | H2 + PostgreSQL |
| invalid date string, missing type and unknown type -> 400 validation_failed with the allowed values | `validation_failed` | H2 + PostgreSQL |

## api.PatientApiTest

POST/GET patients, normalisation, pagination of a patient's appointments.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| creates a patient -> 201 with Location; name is trimmed and email lower-cased; blocked is derived | rule 8, 201 Location | H2 + PostgreSQL |
| blank name and malformed email are reported together -> 400 validation_failed | `validation_failed` | H2 + PostgreSQL |
| unknown id -> 404 patient_not_found; non-UUID id -> 404 | `patient_not_found` | H2 + PostgreSQL |
| pages newest-first with page/pageSize and reports the total | pagination | H2 + PostgreSQL |
| page < 1, pageSize > 100 or non-numeric values -> 400 validation_failed | pagination, `validation_failed` | H2 + PostgreSQL |
| unknown patient -> 404 | - | H2 + PostgreSQL |

## api.AppointmentApiTest

The appointment lifecycle over HTTP: rule violations with codes, transitions, cancellation policy, no-show blocking, reschedule atomicity.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| books -> 201 with Location, status Booked, one history entry, version 0; GET includes the history | rule 11, 201 Location | H2 + PostgreSQL |
| rule violations surface with the documented status and code | - | H2 + PostgreSQL |
| unknown practitioner or patient -> 404 with the entity in the code | - | H2 + PostgreSQL |
| Booked -> CheckedIn -> Completed with a history entry per step | rule 11 | H2 + PostgreSQL |
| complete from Booked, check-in twice, cancel after Completed -> 409 invalid_transition | rule 7, `invalid_transition` | H2 + PostgreSQL |
| a checked-in appointment can be cancelled by the clinic but not by the patient | rule 7 | H2 + PostgreSQL |
| unknown appointment -> 404 appointment_not_found on every action | `appointment_not_found` | H2 + PostgreSQL |
| 48h before -> Cancelled by patient, not late, counter untouched | rule 7 | H2 + PostgreSQL |
| between 24h and 2h before -> late cancellation and the patient's counter increments | rule 7 | H2 + PostgreSQL |
| less than 2h before -> 422 cancellation_window_closed and the appointment stays Booked | rule 7, rule 1, `cancellation_window_closed` | H2 + PostgreSQL |
| cancelling an already cancelled appointment -> 409 invalid_transition | rule 7, `invalid_transition` | H2 + PostgreSQL |
| a note longer than 500 characters -> 400 validation_failed | `validation_failed` | H2 + PostgreSQL |
| before the start -> 409 no_show_before_start; after the start -> NoShow recorded on the patient | `no_show_before_start` | H2 + PostgreSQL |
| no-show on a cancelled or completed appointment -> 409 invalid_transition | rule 8, rule 7, `invalid_transition` | H2 + PostgreSQL |
| the third no-show within 90 days blocks the patient: booking and waitlist -> 403 patient_blocked with blockedUntil; the block lifts at blockedUntil | rule 9, rule 8, auth, `patient_blocked` | H2 + PostgreSQL |
| moves the appointment: old Cancelled with a note, new Booked, both linked in history notes | rule 11, rule 7 | H2 + PostgreSQL |
| when the new slot is invalid nothing changes: old stays Booked, no history entry, version unchanged | rule 11 | H2 + PostgreSQL |
| a patient rescheduling inside the late window pays the late-cancellation penalty; inside the closed window it is refused | rule 10, rule 7, rule 1 | H2 + PostgreSQL |
| rescheduling to the identical slot is a no-op that returns 200 with the same appointment | rule 10 | H2 + PostgreSQL |
| can move to another practitioner and change the type | - | H2 + PostgreSQL |
| validation errors for newStart without offset, bad practitioner id and unknown type are collected | - | H2 + PostgreSQL |
| a retroactively recorded no-show is stored exactly once and still triggers the block (no-shows derived from appointments) | rule 8 | H2 + PostgreSQL |
| rescheduling a cancelled or completed appointment to its own slot is not a no-op: 409 invalid_transition | rule 10, rule 7, `invalid_transition` | H2 + PostgreSQL |

## api.WaitlistApiTest

Rule 9: joining, listing, lazy expiry, FIFO promotion with skips, promotion inside the cancel transaction and the documented failure decision.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| joins -> 201 with Location, GET /waitlist/{id} and the FIFO listing show it as Waiting | rule 9, 201 Location | H2 + PostgreSQL |
| joining twice for the same practitioner and date -> 409 waitlist_duplicate | rule 9, `waitlist_duplicate` | H2 + PostgreSQL |
| past date, date beyond the horizon and a day off -> 422; missing/invalid fields -> 400 | rule 4 | H2 + PostgreSQL |
| listing requires practitionerId and date, supports paging, and 404s for unknown practitioner or entry | pagination | H2 + PostgreSQL |
| entries whose date has passed are reported as Expired on read | - | H2 + PostgreSQL |
| FIFO: with three waiting entries the oldest gets the freed slot; the promoted appointment is flagged and the entry Fulfilled | rule 9 | H2 + PostgreSQL |
| a blocked first entry and a first entry whose patient already has an appointment that day are skipped | rule 8 | H2 + PostgreSQL |
| a freed follow-up slot is too short for a waitlisted procedure: it is skipped and the next fitting entry is promoted | rule 9 | H2 + PostgreSQL |
| expired entries are never promoted (clinic cancels a past appointment) | rule 9, rule 7 | H2 + PostgreSQL |
| nothing waiting -> cancellation without promotion; a reschedule also promotes into the freed slot | rule 9, rule 10, rule 7 | H2 + PostgreSQL |
| promotion runs in the cancel transaction: both the cancellation and the promoted booking are visible together | rule 9, rule 7 | H2 + PostgreSQL |
| if promotion itself throws, the cancellation is still committed (documented decision) | rule 9, rule 7 | H2 + PostgreSQL |

## api.ProblemMappingTest

One case per DomainException subtype for the exhaustive status mapping, plus every framework failure (malformed JSON, 415, 413, 500 without leaks).

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| ValidationException(invalid_thing) -> 400 | - | H2 + PostgreSQL |
| UnauthenticatedException(unauthenticated) -> 401 | auth, `unauthenticated` | H2 + PostgreSQL |
| ForbiddenException(forbidden_role) -> 403 | auth, `forbidden_role` | H2 + PostgreSQL |
| ForbiddenException(not_owner) -> 403 | auth, `not_owner` | H2 + PostgreSQL |
| PatientBlockedException(patient_blocked) -> 403 | rule 8, auth, `patient_blocked` | H2 + PostgreSQL |
| NotFoundException(patient_not_found) -> 404 | `patient_not_found` | H2 + PostgreSQL |
| InvalidTransitionException(invalid_transition) -> 409 | `invalid_transition` | H2 + PostgreSQL |
| ConflictException(slot_taken) -> 409 | `slot_taken` | H2 + PostgreSQL |
| ConflictException(patient_conflict) -> 409 | `patient_conflict` | H2 + PostgreSQL |
| ConflictException(daily_capacity_reached) -> 409 | rule 6, `daily_capacity_reached` | H2 + PostgreSQL |
| ConflictException(waitlist_duplicate) -> 409 | rule 9, `waitlist_duplicate` | H2 + PostgreSQL |
| ConflictException(no_show_before_start) -> 409 | `no_show_before_start` | H2 + PostgreSQL |
| ConcurrencyException(concurrent_modification) -> 409 | `concurrent_modification` | H2 + PostgreSQL |
| RuleViolationException(outside_working_hours) -> 422 | `outside_working_hours` | H2 + PostgreSQL |
| RuleViolationException(slot_misaligned) -> 422 | rule 1, `slot_misaligned` | H2 + PostgreSQL |
| RuleViolationException(practitioner_unavailable) -> 422 | `practitioner_unavailable` | H2 + PostgreSQL |
| RuleViolationException(outside_booking_horizon) -> 422 | rule 4, `outside_booking_horizon` | H2 + PostgreSQL |
| RuleViolationException(cancellation_window_closed) -> 422 | rule 7, rule 1, `cancellation_window_closed` | H2 + PostgreSQL |
| an unhandled exception -> 500 internal_error without leaking the message or a stack trace | `internal_error` | H2 + PostgreSQL |
| a domain exception thrown from any route is mapped, with blockedUntil rendered in the clinic zone | rule 8 | H2 + PostgreSQL |
| a unique-constraint SQL exception -> 409 conflict, other SQL exceptions -> 500 | `conflict` | H2 + PostgreSQL |
| malformed JSON body -> 400 malformed_request | `malformed_request` | H2 + PostgreSQL |
| empty body on an endpoint that needs one -> 400 malformed_request, not 500 | `malformed_request` | H2 + PostgreSQL |
| wrong field types in JSON -> 400 malformed_request | `malformed_request` | H2 + PostgreSQL |
| a non-JSON content type -> 415 unsupported_media_type | `unsupported_media_type` | H2 + PostgreSQL |
| a deadlock (40P01) or serialization failure (40001) -> 409 concurrent_modification, retryable | `concurrent_modification` | H2 + PostgreSQL |

## api.OperationsTest

Health endpoints, metrics, correlation ids, rate limiting, the development token issuer.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| GET /health/live -> 200 UP without touching the database | - | H2 + PostgreSQL |
| GET /health/ready -> 200 with database UP after a real SELECT 1 | - | H2 + PostgreSQL |
| GET /health -> same as readiness | - | H2 + PostgreSQL |
| GET /metrics -> Prometheus text exposition including ktor http metrics | - | H2 + PostgreSQL |
| X-Correlation-Id from the request is echoed back and included in problem responses | - | H2 + PostgreSQL |
| a missing X-Correlation-Id is generated by the server | - | H2 + PostgreSQL |
| unknown route -> 404 route_not_found | `route_not_found` | H2 + PostgreSQL |
| wrong method on a known route -> 405 method_not_allowed | `method_not_allowed` | H2 + PostgreSQL |
| body above MAX_BODY_BYTES -> 413 payload_too_large | `payload_too_large` | H2 + PostgreSQL |
| the third POST inside one minute from the same client -> 429 rate_limited, GETs are not limited | `rate_limited` | H2 + PostgreSQL |
| POST /auth/token mints a usable bearer token when AUTH_DEV_ISSUER_ENABLED=true | auth | H2 + PostgreSQL |
| POST /auth/token validates subject and role -> 400 validation_failed with field errors | auth, `validation_failed` | H2 + PostgreSQL |
| POST /auth/token does not exist when the dev issuer is disabled -> 404 | auth | H2 + PostgreSQL |
| authenticated POSTs are limited per subject, so one busy client does not block another | - | H2 + PostgreSQL |
| X-Forwarded-For separates anonymous clients only when TRUST_PROXY_HEADERS=true | - | H2 + PostgreSQL |

## api.ConcurrencyTest

Real parallel HTTP requests against PostgreSQL: row locks, partial unique index, optimistic version column. Skipped on H2.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| 10 parallel bookings for the same slot -> exactly one 201, the rest 409 slot_taken (practitioner row lock + partial unique index) | concurrency, `slot_taken` | PostgreSQL |
| parallel bookings on a practitioner with maxAppointmentsPerDay = 1 -> exactly one succeeds (capacity is checked under the row lock) | concurrency, rule 6 | PostgreSQL |
| one patient booking four practitioners at the same time in parallel -> exactly one succeeds (patient row lock serialises rule 5) | rule 5, concurrency | PostgreSQL |
| two parallel cancellations of one appointment -> one 200 and one 409 (version column or state machine), history grows by exactly one | concurrency, rule 11, rule 7 | PostgreSQL |
| parallel check-in and clinic cancel on one appointment -> either one loses with 409, or they serialise into Booked -> CheckedIn -> Cancelled; never a 5xx | concurrency, rule 7 | PostgreSQL |
| 20x: a late-window reschedule racing a booking for the same patient and practitioner never deadlocks (no 500; locks taken in one order) | rule 10, rule 1 | PostgreSQL |
| two no-shows for the same patient marked in parallel are both recorded (patient row lock, no lost update) | concurrency, rule 8 | PostgreSQL |
| 5 parallel waitlist joins for the same patient, practitioner and date -> exactly one 201, the rest 409 waitlist_duplicate (patient lock + partial unique index) | concurrency, rule 9, `waitlist_duplicate` | PostgreSQL |

## infrastructure.ExposedRepositoriesTest

Every repository method against the database, status codec, optimistic locking, the partial unique index (PostgreSQL).

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| save + findById round-trips schedule and settings; unknown id is null | - | H2 + PostgreSQL |
| saving again updates settings and replaces the working hours | - | H2 + PostgreSQL |
| lockForBooking on existing practitioner and patient rows succeeds inside a transaction | - | H2 + PostgreSQL |
| forPractitionerBetween returns only intersecting blocks, ordered by start | - | H2 + PostgreSQL |
| save + findById round-trips late cancellations and blockedUntil; unknown id is null | rule 8, rule 7 | H2 + PostgreSQL |
| no-shows are derived from NoShow appointments (oldest first), never stored a second time | rule 8 | H2 + PostgreSQL |
| insert, read with history, and range queries by practitioner and patient | rule 11 | H2 + PostgreSQL |
| update persists the new status, appends history and bumps the version; a stale version -> ConcurrencyException | rule 11 | H2 + PostgreSQL |
| forPatient pages newest first with a total | pagination | H2 + PostgreSQL |
| every status round-trips through the codec | - | H2 + PostgreSQL |
| StatusCodec rejects inconsistent rows | - | H2 + PostgreSQL |
| a unique violation on insert is mapped to ConflictException slot_taken (PostgreSQL partial index) | `slot_taken` | H2 + PostgreSQL |
| save, findById, FIFO listing by practitioner and date, and status update | - | H2 + PostgreSQL |

## infrastructure.DatabaseFactoryTest

Startup retry budget, readiness ping, idempotent migrations, transaction rollback and savepoint semantics.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| connect against an unreachable database retries within the budget and then fails with a clear message | - | H2 + PostgreSQL |
| connect against a reachable database migrates and answers ping; a closed pool answers false | - | H2 + PostgreSQL |
| migrate is idempotent: running it twice applies nothing the second time | - | H2 + PostgreSQL |
| a failure after the first insert rolls the whole transaction back: nothing is persisted | - | H2 + PostgreSQL |
| a savepoint failure rolls back only its own writes; the outer transaction commits | - | H2 + PostgreSQL |
| a successful savepoint keeps its writes | - | H2 + PostgreSQL |
| the return value of the block is returned | - | H2 + PostgreSQL |

## infrastructure.PostgresSchemaTest

Vendor-specific migration and partial index exist on PostgreSQL.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| the vendor migration created the partial unique index on active appointments | - | PostgreSQL |

## AppConfigTest

Environment parsing: defaults, every variable, OIDC mode selection, all problems reported together.

| Test | Rules / codes exercised | Database |
|------|-------------------------|----------|
| minimal environment yields the documented defaults | - | none (pure unit test) |
| every variable is honoured when set | - | none (pure unit test) |
| JWKS mode is selected when the three OIDC variables are present | - | none (pure unit test) |
| all problems are reported at once: missing DATABASE_URL, short signing key, bad numbers, bad zone, bad boolean | - | none (pure unit test) |
| a non-PostgreSQL DATABASE_URL, a partial JWKS setup, a non-https JWKS URL and dev issuer in JWKS mode are rejected | - | none (pure unit test) |
